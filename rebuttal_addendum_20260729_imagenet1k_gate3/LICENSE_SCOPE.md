# License and redistribution scope

This addendum contains derived scalar accuracies, paired deltas, protocol
metadata, hashes, and a CPU-only statistical verifier.  It intentionally
omits ImageNet images/labels, the full ImageNet train score array, raw logs,
classifier checkpoints, and the `stabilityai/sd-vae-ft-mse` weights.

Reproduction requires authorized access to ImageNet-1K and the upstream VAE
model.  ImageNet access conditions and model-card/license terms remain
applicable.  Inclusion of derived values is not a claim of unrestricted
redistribution or legal advice; institutional review should confirm the
release scope before publication.
