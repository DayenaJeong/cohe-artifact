**Artifact addendum.** In response to the reviewer, we added a clearly separated
author-response addendum to the existing anonymous artifact containing seven
verified CIFAR-100 scalar proxy arrays with sample-order manifests, together
with DINOv2 multi-budget and DDPM full-data-ordering materials and CPU-only
evaluation scripts. We do not redistribute ImageNet-1K arrays because the
applicable scalar-only terms were not sufficiently clear to claim safe release.
